# CodeCatalog · backlog

What is next. An empty backlog means finished, and that is information.

- [ ] three-things sits on its own shell: 1231 wide, heading at 73, 1062 measure. Decide
      whether to bring it onto the shared one. The measure is source-backed and will not fit 770.
- [ ] Transactions (mobile) is missing the navy ✕ at the right end of the filter row. The
      Figma export has it; the build never did.
- [ ] three-things age slider is drawn, not wired. The two source points (34 → £1,643,
      21 → £3,440) do not fit one growth rate, so it needs the real model first.
- [ ] Give the 19 older screens a `flow` in their meta.json (the desktop set reads as one sign-up
      flow; the older mobile set is the logged-in app). They show under "All flows" until then.
- [ ] Add the Dashboard pair (desktop 1200×812 and mobile 375×812, Figma section 96:9520) - built
      with figma2code on 2026-09-21 but not yet imported. It waits in `figma2code/dashboard/`.
